<div class="new_item">
  <div class="new_item__title">
    <h3>編集画面</h3>
  </div>
  <div class="new_item__form">
    <%= form_with model: @item, local:true do |f| %>
      <%# エラーメッセージ %>
      <%= render "errors", model: f.object %>
      <%# form %>
      <%= render 'itemform', { f: f } %>
    <% end %>
  </div>
</div>
