$(function() {
  $('.slider').slick({
    // 矢印表示
    arrows: false,
    cssEase: 'linear',
    // autoplaySpeed: 0,
    // speed: 7000,

    speed: 1000,
    fade: true,

    // 自動スライド
    autoplay:true
  })
});

